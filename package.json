{
  "name": "nuxt-101",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "export": "nuxt export",
    "generate": "nuxt generate",
    "serve": "nuxt serve",
    "production": "npm run build && nuxt generate"
  },
  "dependencies": {
    "@nuxtjs/cloudinary": "^1.0.3-beta-1",
    "@nuxtjs/sitemap": "^2.4.0",
    "nuxt": "^2.14.7"
  },
  "devDependencies": {
    "@nuxtjs/style-resources": "^1.0.0",
    "node-sass": "^4.14.1",
    "sass-loader": "^10.0.3"
  }
}
